// src/features/NumberGenerator/types/LotteryNumber.ts

/**
 * Interface representing a lottery number.
 * Each lottery number has an id and value.
 */
export interface LotteryNumber {
  id: string;
  value: number;
}
